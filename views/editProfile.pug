extends layouts/main

block content
    .form-container
        form(action=`/users${routes.editProfile}`, method="post", enctype="multipart/form-data")
            .fileUpload
                label(for="avatar") Avatar
                input(type="file", id="avatar", name="avatar", accept="image/*")
            span User Name:
            input.edit-name(type="text", placeholder="Name", name="name", value=loggedUser.name)
            span Email:
            input.edit-email(type="email", placeholder="Email", name="email", value=loggedUser.email)
            input(type="submit", value="Update Profile")